import { WineColorBackgroundDirective } from './wine-color-background.directive';

describe('WineColorBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new WineColorBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
